<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Set the corner radius for rounded edges -->
    <corners android:radius="25dp" />

    <!-- Background color -->
    <solid android:color="@color/Aquamarine" />

    <!-- Border outline -->
    <stroke
        android:width="7dp"
        android:color="@color/Hunters_Green" />

    <!-- Padding inside the SearchView -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>